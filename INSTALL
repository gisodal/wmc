In order to initialize and clone external repositories, type:
> ./scripts/git-init-all

To get the latest version of external repositories, type:
>./scripts/git-pull-all

To build, type:
> make

Binaries will be installed in the '<path/to/source>/bin' folder.

